{
  "name": "@nous/subcortex-nudges",
  "version": "0.0.1",
  "private": true,
  "description": "Canonical discovery nudge runtime for Nous-OSS",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --config vitest.config.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@nous/shared": "workspace:*"
  },
  "devDependencies": {
    "@nous/subcortex-registry": "workspace:*",
    "vitest": "^2.1.0"
  }
}
