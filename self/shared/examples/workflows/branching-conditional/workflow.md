---
name: branching-conditional
description: Conditional composite workflow package with explicit true and false branch outputs.
entrypoint: webhook-trigger
entrypoints:
  - webhook-trigger
---

# Branching Conditional

This example demonstrates a topology that uses the `output` field on
connections to model explicit branching from a condition node.
