---
nous:
  v: 2
  kind: workflow_node
  id: merge-output
---

# Merge Output

Persist the selected branch result to a stable artifact location so later
systems can consume a single merged output regardless of which branch ran.
